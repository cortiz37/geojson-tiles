html
    include include/header-server
    body
        block top
        div(id='map')
            script(type='text/javascript').
                const tiles = "#{tiles}";

            block main
                script(type='text/javascript').
                    requestLayers = "#{requestLayers}";
                    generateLayers()